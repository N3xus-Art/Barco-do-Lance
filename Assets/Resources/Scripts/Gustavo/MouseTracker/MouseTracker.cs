using UnityEngine;

public class MouseTracker : MonoBehaviour
{
    void Update()
    {
       /* // Get the mouse position in screen coordinates
        Vector3 mouseScreenPosition = Input.mousePosition;

        // Convert the screen position to world coordinates
        Vector3 mouseWorldPosition = Camera.main.ScreenToWorldPoint(new Vector3(
            mouseScreenPosition.x,
            mouseScreenPosition.y,
            Camera.main.nearClipPlane // Distance from the camera to the near clipping plane
        ));

        // Log the position to the console
        Debug.Log("Mouse Position (World): " + mouseWorldPosition); */
    }
}
